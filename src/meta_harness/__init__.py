"""borromeanRings / meta_harness — the governing quality layer.

v0 contains a single real module (:mod:`meta_harness.sample`) so the gate has
genuine code to verify. Real harness modules replace it as the project grows.
"""

__version__ = "0.0.0"
