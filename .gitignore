# Python
__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
.venv/
venv/

# borromeanRings audit plumbing — per-run artifacts (receipts, stop counters, merge logs), not source
.meta-harness/

# CS130 knowledge base — reference material that informed the build, not borromeanRings's product code.
# Untracked by default; remove this line if you want it versioned with the repo.
cs130/
